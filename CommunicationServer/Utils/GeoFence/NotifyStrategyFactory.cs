using System;
using System.Collections.Generic;
using System.Text;
using GlobSaldo.AVL.Entities;

namespace Taxi.Communication.Server.Utils.GeoFence
{
    public class NotifyStrategyFactory
    {
        ////public static INotifyStrategy getStrategy(GeoFencesStrategyList strategyType)
        ////{
        ////    switch (strategyType){
        ////        case GeoFencesStrategyList.NotifyIn:
        ////            return new NotifyIn();
        ////        case GeoFencesStrategyList.NotifyOut:
        ////            return new NotifyOut();
        ////        case GeoFencesStrategyList.NotifyEnter:
        ////            return new NotifyEnter();
        ////        case GeoFencesStrategyList.NotifyExit:
        ////            return new NotifyExit();
        ////        default:
        ////            throw new InvalidOperationException();
        ////    }
        ////}
    }
}
